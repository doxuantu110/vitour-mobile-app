<?xml version="1.0" encoding="utf-8"?>
<!--
    bg_chip_default.xml
    ─────────────────────────────────────────────────────────────────
    Default (unselected) state background for filter chips.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/vitour_surface_variant" />
    <corners android:radius="@dimen/radius_chip" />
    <stroke
        android:width="@dimen/stroke_thin"
        android:color="@color/vitour_outline" />
</shape>
